export PATH=~/.sbin:/home/$USER/.local/bin:$PATH
export EDITOR=vim

alias ip.me='curl https://ip.me'
alias ros2.on='source /opt/ros/humble/setup.bash'
alias dhcpdump="sudo tcpdump -vvv -e 'port 67 or port 68' -n"
